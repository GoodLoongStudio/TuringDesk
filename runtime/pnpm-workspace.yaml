packages:
  - .

# pnpm 10+ blocks dependency lifecycle scripts unless they are explicitly reviewed.
# tsx depends on esbuild, which needs its native binary install step.
allowBuilds:
  esbuild: true
