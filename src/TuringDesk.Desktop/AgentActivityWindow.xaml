<Window x:Class="TuringDesk.Desktop.AgentActivityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="图灵执行中心"
        Width="390" Height="500"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Background="#11151D"
        Foreground="{StaticResource TextBrush}"
        FontFamily="Segoe UI Variable, Segoe UI">
    <Border Background="#151A24" BorderBrush="#39445F" BorderThickness="1" CornerRadius="14" Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="图灵执行中心" FontSize="17" FontWeight="SemiBold"/>
                    <TextBlock x:Name="KernelText" Text="DeepSeek Harness" FontSize="10.5" Foreground="#8F9CB5" Margin="0,3,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Width="32" Height="32" Padding="0" Click="Close_Click" ToolTip="关闭">
                    <TextBlock Text="×" FontSize="18"/>
                </Button>
            </Grid>

            <Border Grid.Row="1" Margin="0,14,0,0" Padding="12" Background="#1B2230" BorderBrush="#34405A" BorderThickness="1" CornerRadius="10">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="StatusDot" Width="9" Height="9" CornerRadius="5" Background="#778399" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock x:Name="StatusText" Text="正在连接 Runtime…" FontWeight="SemiBold" FontSize="12.5"/>
                    </StackPanel>
                    <TextBlock x:Name="CurrentPromptText" Text="当前没有执行任务" Margin="0,9,0,0" TextWrapping="Wrap" FontSize="12" Foreground="#D5DAE5" MaxHeight="58"/>
                    <TextBlock x:Name="ElapsedText" Text="" Margin="0,6,0,0" FontSize="10.5" Foreground="#8491A9"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="2" x:Name="ResultCard" Margin="0,10,0,0" Padding="12" Background="#171C27" CornerRadius="10" Visibility="Collapsed">
                <StackPanel>
                    <TextBlock x:Name="ResultTitle" Text="最近结果" FontSize="10.5" Foreground="#8F9CB5"/>
                    <TextBlock x:Name="ResultText" Margin="0,5,0,0" TextWrapping="Wrap" FontSize="11.5" MaxHeight="70"/>
                </StackPanel>
            </Border>

            <Grid Grid.Row="3" Margin="0,14,0,0">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <TextBlock Text="最近执行" FontSize="11" FontWeight="SemiBold" Foreground="#AEB8CB"/>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,7,0,0">
                    <ItemsControl ItemsSource="{Binding History}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="10,8" Margin="0,0,0,6" Background="#1A202B" CornerRadius="8">
                                    <Grid>
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="8"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <Border Width="6" Height="6" CornerRadius="3" Background="{Binding StatusBrush}" VerticalAlignment="Center"/>
                                        <StackPanel Grid.Column="1" Margin="8,0,8,0">
                                            <TextBlock Text="{Binding Prompt}" TextTrimming="CharacterEllipsis" FontSize="11.5"/>
                                            <TextBlock Text="{Binding Detail}" TextTrimming="CharacterEllipsis" FontSize="9.5" Foreground="#7F8CA4" Margin="0,3,0,0"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="2" Text="{Binding Time}" FontSize="9.5" Foreground="#6F7C93" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>

            <TextBlock Grid.Row="4" Text="Harness 是执行内核；TuringDesk 负责桌面状态、权限边界和可见反馈。" TextWrapping="Wrap" FontSize="9.5" Foreground="#68758D" Margin="0,9,0,0"/>
        </Grid>
    </Border>
</Window>
