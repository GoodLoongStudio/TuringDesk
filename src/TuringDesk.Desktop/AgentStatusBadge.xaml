<UserControl x:Class="TuringDesk.Desktop.AgentStatusBadge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TuringDesk.Desktop"
             FontFamily="Segoe UI Variable, Segoe UI">
    <Button Padding="5,0" Click="Status_Click" ToolTip="打开图灵执行中心">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <local:ShellIcon Kind="Agent" Width="14" Height="14" Foreground="#9DAAFF" Margin="0,0,5,0"/>
            <Border x:Name="StatusDot" Width="6" Height="6" CornerRadius="3" Background="#7D899D" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBlock x:Name="StatusText" Text="图灵" FontSize="10.5" VerticalAlignment="Center"/>
        </StackPanel>
    </Button>
</UserControl>
