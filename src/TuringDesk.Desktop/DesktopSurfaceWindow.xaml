<Window x:Class="TuringDesk.Desktop.DesktopSurfaceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TuringDesk Desktop"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        ShowActivated="False"
        Background="#0B0D12"
        Foreground="{StaticResource TextBrush}"
        FontFamily="Segoe UI Variable, Segoe UI"
        AllowDrop="True"
        DragOver="Desktop_DragOver"
        Drop="Desktop_Drop">
    <Grid x:Name="DesktopRoot">
        <Grid.Background>
            <RadialGradientBrush Center="0.18,0.14" GradientOrigin="0.18,0.14" RadiusX="0.9" RadiusY="0.9">
                <GradientStop Color="#20284D" Offset="0"/>
                <GradientStop Color="#101522" Offset="0.34"/>
                <GradientStop Color="#0B0D12" Offset="1"/>
            </RadialGradientBrush>
        </Grid.Background>
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="刷新" Click="Refresh_Click"/>
                <MenuItem Header="新建文件夹" Click="NewFolder_Click"/>
                <MenuItem Header="打开桌面文件夹" Click="OpenDesktopFolder_Click"/>
                <Separator/>
                <MenuItem Header="显示设置" Click="DisplaySettings_Click"/>
                <MenuItem Header="个性化 / 壁纸" Click="Personalize_Click"/>
                <Separator/>
                <MenuItem Header="打开图灵控制中心" Click="ControlCenter_Click"/>
            </ContextMenu>
        </Grid.ContextMenu>

        <Grid Margin="24,20,24,78">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="4,0,4,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="34" Height="34" CornerRadius="10" Background="#243157" Margin="0,0,10,0">
                        <TextBlock Text="✦" FontSize="18" Foreground="#9DAAFF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel>
                        <TextBlock x:Name="DesktopTitleText" Text="图灵桌面" FontSize="16" FontWeight="SemiBold"/>
                        <TextBlock x:Name="MonitorLabelText" Text="AI Native Desktop for Windows" Foreground="#94A0B8" FontSize="11"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel Margin="0,0,18,0">
                        <TextBlock x:Name="ClockText" Text="--:--" FontSize="26" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                        <TextBlock x:Name="DateText" Text="----年--月--日" Foreground="#94A0B8" FontSize="11" HorizontalAlignment="Right"/>
                    </StackPanel>
                    <Button x:Name="ControlCenterButton" Height="38" Padding="14,0" Click="ControlCenter_Click" ToolTip="打开 TuringDesk 控制中心">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="✦" Foreground="#9DAAFF" Margin="0,0,7,0"/>
                            <TextBlock Text="打开图灵"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <ListBox Grid.Row="1"
                     x:Name="DesktopItemsList"
                     ItemsSource="{Binding DesktopItems}"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MouseDoubleClick="DesktopItemsList_MouseDoubleClick"
                     PreviewMouseLeftButtonDown="DesktopItemsList_PreviewMouseLeftButtonDown"
                     PreviewMouseMove="DesktopItemsList_PreviewMouseMove"
                     MouseRightButtonUp="DesktopItemsList_MouseRightButtonUp">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Width" Value="104"/>
                        <Setter Property="Height" Value="100"/>
                        <Setter Property="Margin" Value="4"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="ItemBorder" CornerRadius="10" Background="Transparent" BorderBrush="#35405D" BorderThickness="1" Padding="8">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#2A3146"/>
                                            <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#53638E"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#303A5E"/>
                                            <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#7F8FFF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid ToolTip="{Binding Path}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="52"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid Width="48" Height="48" HorizontalAlignment="Center">
                                <Image Source="{Binding Icon}" Width="42" Height="42" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                <TextBlock Text="{Binding Glyph}" FontSize="22" Foreground="#A9B4FF" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Icon}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                            <StackPanel Grid.Row="1" VerticalAlignment="Top" Margin="0,2,0,0">
                                <TextBlock Text="{Binding Name}" TextAlignment="Center" TextTrimming="CharacterEllipsis" FontSize="11.5" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Subtitle}" TextAlignment="Center" TextTrimming="CharacterEllipsis" Foreground="#8390A8" FontSize="9.5" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border x:Name="DesktopHint" Grid.Row="2" HorizontalAlignment="Left" Background="#181D29" BorderBrush="#30394F" BorderThickness="1" CornerRadius="12" Padding="12,7" Margin="4,14,0,0">
                <TextBlock Text="文件可拖入/拖出桌面 · 右键保留 Windows 式操作 · 底部任务栏可切换窗口或问图灵" Foreground="#9AA5BC" FontSize="10.5"/>
            </Border>
        </Grid>
    </Grid>
</Window>
