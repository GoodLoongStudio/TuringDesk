<Window x:Class="TuringDesk.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:local="clr-namespace:TuringDesk.Desktop"
        Title="TuringDesk · 图灵桌面"
        Width="1180" Height="820"
        MinWidth="900" MinHeight="640"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{StaticResource WindowBrush}"
        Foreground="{StaticResource TextBrush}"
        FontFamily="Segoe UI Variable, Segoe UI">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="48"/><RowDefinition Height="46"/><RowDefinition Height="*"/></Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource StrokeBrush}" BorderThickness="0,0,0,1" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="220"/><ColumnDefinition Width="*"/><ColumnDefinition Width="250"/><ColumnDefinition Width="138"/></Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
                    <Border Width="26" Height="26" CornerRadius="7" Background="{StaticResource AccentSoftBrush}" Margin="0,0,8,0">
                        <local:ShellIcon Kind="Agent" Width="15" Height="15" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="TuringDesk" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>

                <Border Grid.Column="1" Width="460" Height="34" MaxWidth="520" HorizontalAlignment="Center" VerticalAlignment="Center" Background="{StaticResource Surface2Brush}" BorderBrush="{StaticResource StrokeBrush}" BorderThickness="1" CornerRadius="7" MouseLeftButtonDown="CommandBar_MouseLeftButtonDown">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="34"/><ColumnDefinition Width="*"/><ColumnDefinition Width="38"/></Grid.ColumnDefinitions>
                        <local:ShellIcon Kind="Agent" Width="15" Height="15" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" x:Name="CommandBox" Background="Transparent" BorderThickness="0" FontSize="13" Padding="4,6" VerticalContentAlignment="Center" KeyDown="CommandBox_KeyDown" ToolTip="告诉图灵你想做什么，例如：打开 Chrome 和 VS Code，左右排列"/>
                        <Button Grid.Column="2" Padding="0" Margin="3" Click="Ask_Click" ToolTip="发送给图灵"><local:ShellIcon Kind="ArrowRight" Width="15" Height="15" Foreground="{StaticResource AccentBrush}"/></Button>
                    </Grid>
                </Border>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <Ellipse x:Name="RuntimeDot" Width="7" Height="7" Fill="{StaticResource WarningBrush}" Margin="0,0,7,0"/>
                    <TextBlock x:Name="RuntimeStatus" Text="Runtime connecting…" Foreground="{StaticResource MutedBrush}" FontSize="11" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBlock x:Name="ModelStatus" Text="Mock" Foreground="{StaticResource SubtleBrush}" FontSize="11" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource TitleBarButtonStyle}" Click="Minimize_Click" ToolTip="最小化"><local:ShellIcon Kind="Minimize" Width="15" Height="15"/></Button>
                    <Button x:Name="MaximizeButton" Style="{StaticResource TitleBarButtonStyle}" Content="□" Click="Maximize_Click" ToolTip="最大化 / 还原">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <local:ShellIcon Width="15" Height="15">
                                    <local:ShellIcon.Style>
                                        <Style TargetType="local:ShellIcon">
                                            <Setter Property="Kind" Value="Maximize"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource AncestorType=Window}}" Value="Maximized"><Setter Property="Kind" Value="RestoreWindow"/></DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </local:ShellIcon.Style>
                                </local:ShellIcon>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>
                    <Button Style="{StaticResource TitleBarButtonStyle}" Click="Close_Click" ToolTip="关闭"><local:ShellIcon Kind="Close" Width="15" Height="15"/></Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource WindowBrush}" BorderBrush="{StaticResource StrokeBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="10,0">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="NavHomeButton" Style="{StaticResource TopNavButtonActiveStyle}" Click="NavHome_Click"><StackPanel Orientation="Horizontal"><local:ShellIcon Kind="Home" Width="15" Height="15" Margin="0,0,7,0"/><TextBlock Text="首页"/></StackPanel></Button>
                    <Button x:Name="NavAppsButton" Style="{StaticResource TopNavButtonStyle}" Click="NavApps_Click"><StackPanel Orientation="Horizontal"><local:ShellIcon Kind="App" Width="15" Height="15" Margin="0,0,7,0"/><TextBlock Text="应用"/></StackPanel></Button>
                    <Button x:Name="NavWorkspacesButton" Style="{StaticResource TopNavButtonStyle}" Click="NavWorkspaces_Click"><StackPanel Orientation="Horizontal"><local:ShellIcon Kind="Workspace" Width="15" Height="15" Margin="0,0,7,0"/><TextBlock Text="工作区"/></StackPanel></Button>
                    <Button x:Name="NavTasksButton" Style="{StaticResource TopNavButtonStyle}" Click="NavTasks_Click"><StackPanel Orientation="Horizontal"><local:ShellIcon Kind="Tasks" Width="15" Height="15" Margin="0,0,7,0"/><TextBlock Text="任务"/></StackPanel></Button>
                    <Button x:Name="NavMemoryButton" Style="{StaticResource TopNavButtonStyle}" Click="NavMemory_Click"><StackPanel Orientation="Horizontal"><local:ShellIcon Kind="Memory" Width="15" Height="15" Margin="0,0,7,0"/><TextBlock Text="记忆"/></StackPanel></Button>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="MicButton" Style="{StaticResource IconButtonStyle}" Content="&#xE720;" Click="SpeechToggle_Click" ToolTip="常驻语音">
                        <Button.ContentTemplate><DataTemplate><local:ShellIcon Kind="Microphone" Width="16" Height="16"/></DataTemplate></Button.ContentTemplate>
                    </Button>
                    <Button Style="{StaticResource AgentPillButtonStyle}" Click="AgentPanel_Click" Margin="4,0">
                        <StackPanel Orientation="Horizontal"><Ellipse x:Name="AgentStateDot" Width="7" Height="7" Fill="{StaticResource WarningBrush}" Margin="0,0,7,0" VerticalAlignment="Center"/><TextBlock x:Name="AgentStatusText" Text="Agent connecting" VerticalAlignment="Center"/></StackPanel>
                    </Button>
                    <Button Style="{StaticResource IconButtonStyle}" Click="ModelSettings_Click" ToolTip="模型与设置"><local:ShellIcon Kind="Settings" Width="16" Height="16"/></Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid x:Name="PageHost">
                <ScrollViewer x:Name="HomePage" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="28,24,28,40">
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="首页" FontSize="28" FontWeight="SemiBold"/><TextBlock Text="像普通 Windows 一样开始工作，图灵会在需要时接管重复操作。" Foreground="{StaticResource MutedBrush}" FontSize="13" Margin="0,5,0,0"/></StackPanel>
                            <Button Grid.Column="1" Style="{StaticResource PrimaryButtonStyle}" Click="FocusAgent_Click" VerticalAlignment="Center"><StackPanel Orientation="Horizontal"><local:ShellIcon Kind="Agent" Width="16" Height="16" Margin="0,0,7,0"/><TextBlock Text="问图灵"/></StackPanel></Button>
                        </Grid>

                        <Border Style="{StaticResource CardBorderStyle}" Padding="22" Margin="0,0,0,20">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="300"/></Grid.ColumnDefinitions>
                                <StackPanel Margin="0,0,28,0">
                                    <TextBlock Text="继续你的桌面工作" FontSize="20" FontWeight="SemiBold"/>
                                    <TextBlock Text="TuringDesk 保留真实 Windows 应用和窗口习惯，同时让 Agent 负责启动、寻找、恢复和排列。" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" FontSize="13" Margin="0,8,0,18"/>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Style="{StaticResource PrimaryButtonStyle}" Click="VSCode_Click" Margin="0,0,8,0"><StackPanel Orientation="Horizontal"><local:ShellIcon Kind="Code" Width="15" Height="15" Margin="0,0,7,0"/><TextBlock Text="打开 VS Code"/></StackPanel></Button>
                                        <Button Click="Chrome_Click"><StackPanel Orientation="Horizontal"><local:ShellIcon Kind="Browser" Width="15" Height="15" Margin="0,0,7,0"/><TextBlock Text="打开 Chrome"/></StackPanel></Button>
                                    </StackPanel>
                                </StackPanel>
                                <Border Grid.Column="1" Background="{StaticResource AccentSoftBrush}" CornerRadius="10" Padding="16">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal"><local:ShellIcon Kind="Agent" Width="16" Height="16" Foreground="{StaticResource AccentBrush}" Margin="0,0,7,0"/><TextBlock Text="图灵建议" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold"/></StackPanel>
                                        <TextBlock x:Name="WorkspaceSuggestion" Text="试着说：图灵桌面，打开 Chrome 和 VS Code，左右排列。" TextWrapping="Wrap" FontSize="13" Margin="0,10,0,0"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="常用应用" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Style="{StaticResource TopNavButtonStyle}" Click="NavApps_Click"><StackPanel Orientation="Horizontal"><TextBlock Text="查看全部"/><local:ShellIcon Kind="ArrowRight" Width="13" Height="13" Margin="7,0,0,0"/></StackPanel></Button>
                        </Grid>
                        <WrapPanel Margin="0,0,0,14">
                            <Button Style="{StaticResource AppCardButtonStyle}" Click="Chrome_Click"><StackPanel><local:ShellIcon Kind="Browser" Width="28" Height="28" Foreground="{StaticResource AccentBrush}"/><TextBlock Text="Chrome" FontWeight="SemiBold" FontSize="14" Margin="0,10,0,2"/><TextBlock Text="浏览器" Foreground="{StaticResource MutedBrush}" FontSize="11"/></StackPanel></Button>
                            <Button Style="{StaticResource AppCardButtonStyle}" Click="VSCode_Click"><StackPanel><local:ShellIcon Kind="Code" Width="28" Height="28" Foreground="{StaticResource AccentBrush}"/><TextBlock Text="VS Code" FontWeight="SemiBold" FontSize="14" Margin="0,10,0,2"/><TextBlock Text="开发" Foreground="{StaticResource MutedBrush}" FontSize="11"/></StackPanel></Button>
                            <Button Style="{StaticResource AppCardButtonStyle}" Click="Terminal_Click"><StackPanel><local:ShellIcon Kind="Terminal" Width="28" Height="28" Foreground="{StaticResource AccentBrush}"/><TextBlock Text="Terminal" FontWeight="SemiBold" FontSize="14" Margin="0,10,0,2"/><TextBlock Text="系统工具" Foreground="{StaticResource MutedBrush}" FontSize="11"/></StackPanel></Button>
                        </WrapPanel>

                        <TextBlock Text="当前工作区" FontSize="17" FontWeight="SemiBold" Margin="0,4,0,10"/>
                        <Border Style="{StaticResource CardBorderStyle}" Padding="18">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <Border Width="46" Height="46" Background="{StaticResource Surface2Brush}" CornerRadius="10"><local:ShellIcon Kind="Workspace" Width="22" Height="22" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                <StackPanel Grid.Column="2" VerticalAlignment="Center"><TextBlock Text="Desktop Workspace" FontWeight="SemiBold"/><TextBlock Text="应用仍是原生 Windows 窗口；Agent 负责上下文和编排。" Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,4,0,0"/></StackPanel>
                                <Border Grid.Column="3" Background="#183322" CornerRadius="12" Padding="9,4" VerticalAlignment="Center"><TextBlock Text="ACTIVE" Foreground="{StaticResource SuccessBrush}" FontSize="10" FontWeight="SemiBold"/></Border>
                            </Grid>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="AppsPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="28,24,28,40">
                        <TextBlock Text="应用" FontSize="28" FontWeight="SemiBold"/><TextBlock Text="保留传统应用库的使用习惯，同时允许图灵按意图启动和组合应用。" Foreground="{StaticResource MutedBrush}" FontSize="13" Margin="0,5,0,20"/>
                        <WrapPanel>
                            <Button Style="{StaticResource AppCardButtonStyle}" Click="Chrome_Click"><StackPanel><local:ShellIcon Kind="Browser" Width="28" Height="28" Foreground="{StaticResource AccentBrush}"/><TextBlock Text="Chrome" FontWeight="SemiBold" Margin="0,10,0,2"/><TextBlock Text="浏览器" Foreground="{StaticResource MutedBrush}" FontSize="11"/></StackPanel></Button>
                            <Button Style="{StaticResource AppCardButtonStyle}" Click="VSCode_Click"><StackPanel><local:ShellIcon Kind="Code" Width="28" Height="28" Foreground="{StaticResource AccentBrush}"/><TextBlock Text="VS Code" FontWeight="SemiBold" Margin="0,10,0,2"/><TextBlock Text="开发" Foreground="{StaticResource MutedBrush}" FontSize="11"/></StackPanel></Button>
                            <Button Style="{StaticResource AppCardButtonStyle}" Click="Terminal_Click"><StackPanel><local:ShellIcon Kind="Terminal" Width="28" Height="28" Foreground="{StaticResource AccentBrush}"/><TextBlock Text="Terminal" FontWeight="SemiBold" Margin="0,10,0,2"/><TextBlock Text="系统工具" Foreground="{StaticResource MutedBrush}" FontSize="11"/></StackPanel></Button>
                        </WrapPanel>
                    </StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="WorkspacesPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto"><StackPanel Margin="28,24,28,40"><TextBlock Text="工作区" FontSize="28" FontWeight="SemiBold"/><TextBlock Text="把一组应用、窗口布局与上下文作为一个可恢复的工作状态。" Foreground="{StaticResource MutedBrush}" FontSize="13" Margin="0,5,0,20"/><Border Style="{StaticResource CardBorderStyle}"><StackPanel><StackPanel Orientation="Horizontal"><local:ShellIcon Kind="Workspace" Width="18" Height="18" Foreground="{StaticResource AccentBrush}" Margin="0,0,8,0"/><TextBlock Text="Desktop Workspace" FontSize="17" FontWeight="SemiBold"/></StackPanel><TextBlock Text="当前活动 · Chrome / VS Code / Terminal 可由 Agent 编排" Foreground="{StaticResource MutedBrush}" Margin="0,6,0,0"/></StackPanel></Border></StackPanel></ScrollViewer>
                <ScrollViewer x:Name="TasksPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto"><StackPanel Margin="28,24,28,40"><TextBlock Text="任务" FontSize="28" FontWeight="SemiBold"/><TextBlock Text="图灵执行过和正在执行的桌面任务会集中在这里。" Foreground="{StaticResource MutedBrush}" FontSize="13" Margin="0,5,0,20"/><Border Style="{StaticResource CardBorderStyle}"><StackPanel Orientation="Horizontal"><local:ShellIcon Kind="Tasks" Width="18" Height="18" Foreground="{StaticResource AccentBrush}" Margin="0,0,9,0"/><TextBlock Text="实时执行记录先放在 Agent Control Panel；后续这里会成为可恢复任务中心。" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}"/></StackPanel></Border></StackPanel></ScrollViewer>
                <ScrollViewer x:Name="MemoryPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto"><StackPanel Margin="28,24,28,40"><TextBlock Text="记忆" FontSize="28" FontWeight="SemiBold"/><TextBlock Text="让用户明确知道图灵记住了什么，并能够删除或关闭记忆。" Foreground="{StaticResource MutedBrush}" FontSize="13" Margin="0,5,0,20"/><Border Style="{StaticResource CardBorderStyle}"><StackPanel Orientation="Horizontal"><local:ShellIcon Kind="Memory" Width="18" Height="18" Foreground="{StaticResource AccentBrush}" Margin="0,0,9,0"/><TextBlock Text="当前开发预览尚未开放长期个人记忆写入；这里只保留未来的透明控制入口。" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}"/></StackPanel></Border></StackPanel></ScrollViewer>
            </Grid>

            <Border x:Name="AgentPanel" Width="370" HorizontalAlignment="Right" VerticalAlignment="Stretch" Margin="0,12,12,12" Padding="16" Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource StrokeBrush}" BorderThickness="1" CornerRadius="12" Visibility="Collapsed">
                <Border.Effect><DropShadowEffect BlurRadius="24" ShadowDepth="0" Opacity="0.38" Color="#000000"/></Border.Effect>
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center"><Border Width="32" Height="32" Background="{StaticResource AccentSoftBrush}" CornerRadius="16" Margin="0,0,9,0"><local:ShellIcon Kind="Agent" Width="17" Height="17" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><StackPanel><TextBlock Text="Turing Agent" FontSize="15" FontWeight="SemiBold"/><TextBlock Text="Agent Control Panel" FontSize="10" Foreground="{StaticResource MutedBrush}"/></StackPanel></StackPanel>
                        <Button Grid.Column="1" Style="{StaticResource IconButtonStyle}" Click="AgentPanel_Click" ToolTip="关闭"><local:ShellIcon Kind="Close" Width="15" Height="15"/></Button>
                    </Grid>
                    <Border Grid.Row="1" Background="{StaticResource AccentSoftBrush}" CornerRadius="10" Padding="14" Margin="0,14,0,10"><StackPanel><TextBlock x:Name="AgentStateTitle" Text="正在连接能力层…" FontSize="15" FontWeight="SemiBold"/><TextBlock x:Name="AgentStateDetail" Text="图灵会把理解和执行过程显示在这里。" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,5,0,0"/></StackPanel></Border>
                    <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Padding="12" Margin="0,0,0,10"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="常驻语音" FontWeight="SemiBold" FontSize="12"/><TextBlock x:Name="VoiceStateText" Text="正在启动 Windows 语音识别…" Foreground="{StaticResource MutedBrush}" FontSize="10" Margin="0,3,0,0"/></StackPanel><Button Grid.Column="1" x:Name="SpeechButton" Content="启动中" Click="SpeechToggle_Click" Padding="10,6" VerticalAlignment="Center"/></Grid></Border>
                    <Grid Grid.Row="3"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><Grid Margin="2,0,2,7"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="执行记录" FontSize="12" FontWeight="SemiBold"/><TextBlock Grid.Column="1" Text="实时" Foreground="{StaticResource SubtleBrush}" FontSize="10"/></Grid><ListBox x:Name="ActivityList" Grid.Row="1" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled"><ListBox.ItemTemplate><DataTemplate><TextBlock Text="{Binding}" TextWrapping="Wrap" FontSize="11" Foreground="{StaticResource MutedBrush}"/></DataTemplate></ListBox.ItemTemplate></ListBox></Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
