<UserControl x:Class="TuringDesk.Desktop.MonitorProfilesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F5F6F8"
             Foreground="#1F2328"
             FontFamily="Segoe UI Variable, Segoe UI">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="#1F2328"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#C9D0DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="MinHeight" Value="32"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="BorderBrush" Value="#2563EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1F2328"/>
            <Setter Property="BorderBrush" Value="#C9D0DA"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="CaretBrush" Value="#1F2328"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1F2328"/>
            <Setter Property="BorderBrush" Value="#C9D0DA"/>
            <Setter Property="MinHeight" Value="32"/>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="#1F2328"/>
            <Setter Property="Padding" Value="7,5"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="8,14">
        <Grid.ColumnDefinitions><ColumnDefinition Width="280"/><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
        <StackPanel>
            <TextBlock Text="多显示器配置" Foreground="#1F2328" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock Text="每块屏幕独立选择桌面或播放列表。" Foreground="#667085" FontSize="11" Margin="0,4,0,12"/>
            <ListBox x:Name="ProfileList" Height="400" Background="White" BorderBrush="#D9DEE6" BorderThickness="1" DisplayMemberPath="Name" SelectionChanged="ProfileList_SelectionChanged"/>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0"><Button Content="新建配置" Click="NewProfile_Click"/><Button Content="删除" Click="DeleteProfile_Click" Margin="8,0,0,0"/></StackPanel>
        </StackPanel>

        <Border Grid.Column="2" Background="White" BorderBrush="#D9DEE6" BorderThickness="1" CornerRadius="10" Padding="18">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <TextBlock Text="配置内容" Foreground="#1F2328" FontSize="17" FontWeight="SemiBold"/>
                <Grid Grid.Row="1" Margin="0,14,0,14"><Grid.ColumnDefinitions><ColumnDefinition Width="80"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="名称" Foreground="#667085" VerticalAlignment="Center"/><TextBox Grid.Column="1" x:Name="ProfileNameBox"/></Grid>
                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"><StackPanel x:Name="AssignmentPanel"/></ScrollViewer>
                <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,14,0,0"><Button Content="保存配置" Click="SaveProfile_Click"/><Button Content="立即应用到所有屏幕" Click="ActivateProfile_Click" Margin="8,0,0,0" Style="{StaticResource PrimaryButton}"/></StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
